/**
 * Test and development fixtures.
 *
 * Exposed under `@latent/shared/fixtures` rather than the package root so that
 * the production web bundle never pulls them in.
 */
export * from './objectInfo.js';
export * from './workflows.js';
